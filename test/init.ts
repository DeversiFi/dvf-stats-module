import * as chai from 'chai';

// Initialize chai should interface https://www.chaijs.com/guide/styles/#should
chai.should();

before(async () => {
});

after(async () => {
});

afterEach(async () => {
});
